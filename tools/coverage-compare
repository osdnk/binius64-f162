#!/bin/bash
# Wrapper for coverage-compare.py with automatic venv management

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/run_tool.sh" "$SCRIPT_DIR/coverage-compare.py" "$SCRIPT_DIR/requirements/no_reqs.txt" "$@"